name: llm-olama

services:
  ollama:
    image: ollama/ollama
    container_name: ollama
    environment:
      - OLLAMA_CONTEXT_LENGTH=32000
    volumes:
      - ollama:/root/.ollama
    ports:
      - 11434:11434

volumes:
  ollama:
    external: true
    name: ollama
